# Python bytecode cache (auto-generated)
__pycache__/
*.pyc

# Personal user data - your actual goals/habits, not app code.
# Keep this out of version history; it's private and changes constantly.
userData/

# Personal, machine-local Claude Code settings (not shared across machines).
# Note: this is about NOT logging a personal file. Shared dev tooling (tests/,
# .claude/commands/) is committed but kept out of deploys via .gitattributes.
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Momentum 2.0 toolchain
node_modules/
frontend/dist/
**/.env
**/.env.local
backend/.venv/
backend/dev.db
backend/test.db
.pytest_cache/
coverage/

