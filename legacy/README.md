# Legacy Momentum 1.x

Frozen reference copy of the pre-rewrite app (Alpine.js single file + stdlib Python server).
The runnable 1.x app lives on the `main` branch; this folder exists on `momentum-2.0` so the engine port can be checked against the original logic (`app.js`) and tests (`tests/app.test.js`).

Do not edit anything in here.
